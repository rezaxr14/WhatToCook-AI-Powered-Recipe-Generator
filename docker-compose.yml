# ============================================================
# WhatToCook — base stack (works on any direct-internet machine)
#
#   docker compose up -d --build
#
# Behind a forced HTTP proxy? Add your network overrides on top:
#   docker compose -f docker-compose.yml -f docker-compose.proxy.yml up -d --build
# ============================================================

x-backend-common: &backend-common
  build:
    context: .
  image: whattocook-backend:latest
  env_file:
    - .env
  environment:
    DJANGO_SETTINGS_MODULE: WhatToCook.settings
    PYTHONUNBUFFERED: "1"
    PYTHONDONTWRITEBYTECODE: "1"
    # Inside this network we always talk to the postgres service:
    USE_POSTGRES: "true"
    DB_HOST: db
    DB_PORT: "5432"
    CELERY_BROKER_URL: redis://redis:6379/0
    CELERY_RESULT_BACKEND: redis://redis:6379/0
    REDIS_URL: redis://redis:6379/1

services:
  web:
    <<: *backend-common
    container_name: whattocook_web
    command: ["web"]
    ports:
      - "8000:8000"
    volumes:
      - media_data:/app/media
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    image: whattocook-frontend:latest
    container_name: whattocook_frontend
    ports:
      - "4173:80"
    depends_on:
      web:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:15-alpine
    container_name: whattocook_db
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-whattocook}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-secret}
      POSTGRES_DB: ${POSTGRES_DB:-whattocook}
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-whattocook} -d $${POSTGRES_DB:-whattocook}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: whattocook_redis
    # Ephemeral broker/cache is fine: Celery results are advisory,
    # durable data lives in Postgres.
    command: ["redis-server", "--maxmemory", "64mb", "--maxmemory-policy", "allkeys-lru", "--save", ""]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  celery:
    <<: *backend-common
    container_name: whattocook_celery
    command: ["celery"]
    # The image healthcheck probes HTTP; it only applies to the web role.
    healthcheck:
      disable: true
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  telegram_bot:
    <<: *backend-common
    container_name: whattocook_telegram_bot
    command: ["bot"]
    healthcheck:
      disable: true
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  db_data:
  media_data:
